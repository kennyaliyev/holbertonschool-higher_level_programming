# Python - Abstract Base Classes (ABC)

This directory contains exercises on Abstract Base Classes in Python using the `abc` module.

- `task_00_abc.py`: Defines an abstract `Animal` class with `Dog` and `Cat` subclasses that implement the `sound()` method.
